#include "main.h"

/**
 * add - add two numbers
 *
 *@a: the first number to be added
 *@b: the second number to be added
 * Return: Returns the total
 */
int add(int a, int b)
{
	int total;

	total = a + b;
	return (total);
}
