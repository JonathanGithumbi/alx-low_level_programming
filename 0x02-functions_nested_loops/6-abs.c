#include "main.h"
/**
 * _abs - computes the absolute value of an iteger
 *
 *
 * @num: number to be checked
 * Return: abs
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}

