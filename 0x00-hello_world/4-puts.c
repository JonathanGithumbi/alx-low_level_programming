#include <stdio.h>

/*
 *The main function printrs a string using puts and returns 
 *Return: Always 0 (Success)
 * */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
